kind = "web"
previewPath = "/"
title = "CampusDesk – Student Management Portal"
description = "A beginner-friendly student records app for adding, searching, editing, and deleting students with data saved in the browser."
version = "1.0.0"
id = "artifacts/student-management-system"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22847

[services.development]
run = "pnpm --filter @workspace/student-management-system run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/student-management-system", "run", "build" ]
serve = "static"
publicDir = "artifacts/student-management-system/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22847"
BASE_PATH = "/"
